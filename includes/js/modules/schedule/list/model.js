var Options=Backbone.Model.extend({idAttribute:"id",url:function(){if(this.isNew()){return BASE_URL+"api/schedule"}else{return BASE_URL+"api/schedule/id/"+this.get("id")}},defaults:{vessel_id:"",control_nos:0}});var TypeAheadCollection=Backbone.Collection.extend({model:Options});